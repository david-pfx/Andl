// Borg model - option

option := {{ name:'', value }}

// set option
put_option_id(id, opt:option) => do {
    option := [ ?(name = id) ]
    option := union opt [ ?(name <> '' and value <> '') ]
}

// Find option by id
get_option_id(id) => do {
    option[ ?(name = id) ]
}

// Find all options
get_option() => do {
    option
}

