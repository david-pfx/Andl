addresses := {{   address_num:0, 
  first_name, last_name, nickname, email, screen_name, work_phone, home_phone, 
  fax, pager, street, city, state, zip, country, company, work_street, work_city, 
  work_state, work_zip, work_country, webpage, notes, birthday:time, cell_phone
}}

addresses

addresses_add(a:addresses) => do { 
  addresses := union a
}
